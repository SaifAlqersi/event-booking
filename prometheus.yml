global:
  scrape_interval: 15s

scrape_configs:

  - job_name: "event-booking"

    metrics_path: "/actuator/prometheus"

    static_configs:

      - targets:
          - "event-booking-production:8080"